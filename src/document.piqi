.enum [
  .name document-binding
  .option [
    .name early
  ]
  .option [
    .name late
  ]
]

.record [
  .name document

  .field [
    .name name
    .type string
    .optional
  ]

  .field [
    .name binding
    .type document-binding
    .optional
    .default.early
  ]

  .field [
    .name initial-transition
    .type transition
  ]

  .field [
    .name states
    .type state
    .repeated
  ]

  .field [
    .name datamodel
    .type param
    .repeated
  ]
]
