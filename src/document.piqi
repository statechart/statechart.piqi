.record [
  .name document

  .field [
    .name name
    .type string
    .optional
  ]

  .field [
    .name states
    .type state
    .repeated
  ]

  .field [
    .name transitions
    .type transition
    .repeated
  ]
]
