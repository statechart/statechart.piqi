.record [
  .name document

  .field [
    .name name
    .type string
    .optional
  ]

  .field [
    .name initial-transitions
    .type transition
    .repeated
  ]

  .field [
    .name states
    .type state
    .repeated
  ]
]
